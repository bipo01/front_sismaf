function RelatorioSection({ children }) {
	return (
		<section className="relatorio-section">
			<div className="tabelas-container">{children}</div>
		</section>
	);
}

export default RelatorioSection;
